#include<stdio.h>

#include<conio.h>
void main()
{
    int n,fact=1;
    printf("enter a no");
    scanf("%d",&n);
    for(int i=n;i>0;i--)
    {
        fact=fact*i;
    }
    printf("factorial of %d is %d",n,fact);
}
